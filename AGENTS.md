# Repository guide

This repository contains independent Embedded C exercises and the Impactd lab.
For work inside `impactd-lab/`, read `impactd-lab/AGENTS.md` before editing.
Its check command covers that lab, not every legacy exercise in `src/`.
Keep unrelated exercises out of a lab change. Base feature branches on `Main`.
